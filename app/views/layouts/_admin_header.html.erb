<section class="page-header">
  <div class="container">
    <%= link_to(root_path) do %>
    <h2 class="header-bnr">Digsy Admin Dashboard</h2>
    <% end %>
    <span class="pull-right">
      <% if user_signed_in?  && current_user.admin? %>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-md pull-right header-btn" %>
        <%= link_to "Dashboard", actions_path, class: "btn btn-primary btn-md pull-right header-btn" %>
      <% end %>
    </span>
  </div>
</section>
